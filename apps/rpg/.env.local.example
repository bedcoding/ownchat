# 이 파일을 `.env.local` 로 복사해서 값을 채운다. `.env.local` 은 gitignore 된다.
#
# 시나리오 텍스트에는 `{PLATFORM}` 토큰만 커밋되어 있고, 화면에 그릴 때 아래 값으로
# 치환된다. 값을 주지 않으면 가상의 이름(먹줄)으로 떨어지므로, 저장소를 클론한
# 사람도 깨진 문자열 없이 완결된 게임을 보게 된다.
#
# 사내 시연에서만 실제 플랫폼 이름을 넣어 쓴다. 실명을 이 예시 파일에 적지 말 것.

NEXT_PUBLIC_PLATFORM_NAME=우리회사이름
